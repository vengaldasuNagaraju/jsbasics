let x=1;

console.log(x > 0);

console.log(x >= 1);

console.log(x < 1);

console.log(x <= 1);

// we will get the boolean output  

console.log(x!=2);

//lose equality (value)
// in this we didnt check the type of the variable we only check both values are same or not it will change the right side thing in he same as the left side exist.

console.log(x==1); //true
console.log('1' == 1); // true
console.log(true == 1); // true
//strict equality (type +value)
// in these we will chec the vallue and type also both we check 

console.log(x===1);
